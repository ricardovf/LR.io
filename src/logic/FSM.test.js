import Grammar from './Grammar';
import { EPSILON, ACCEPT_STATE } from './SymbolValidator';

describe('FSM', () => {
  describe('recognize', () => {
    it('should recognize aa pairs on an aa pairs language', () => {
      // @todo
    });
  });
});
